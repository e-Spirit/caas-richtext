{
  "name": "typescript-node",
  "version": "0.0.1",
  "description": "Example rich text migrator, that migrates from classic default format to the proposed format",
  "main": "index.js",
  "scripts": {
    "build": "tsc --project .",
    "build-and-run": "tsc --project . && node index.js",
    "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nicolai McAlley",
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  }
}
